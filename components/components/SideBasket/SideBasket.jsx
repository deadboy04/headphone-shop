import {
    Typography,
    Grid,
    Button,
    Link,
    IconButton,
    Dialog,
} from '@material-ui/core';
import React from 'react';
import {useStyles} from "./SideBasket.style";


export default function SideBasket() {
    const classes = useStyles();


    return (anchor) => (
            <main>
                <div>
                </div>
            </main>
    )
}

